[server]
maxUploadSize = 200   # MB — large enough for STIG zips / templates

[theme]
base = "dark"
primaryColor = "#7c5cff"
backgroundColor = "#0b1020"
secondaryBackgroundColor = "#161c2e"
textColor = "#e6e9ef"
font = "sans serif"
