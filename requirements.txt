# Foundation layer is stdlib-only -- nothing required to run db/loader/crosswalk.
# These are for the NEXT layers (LLM responder, artifacts, UI).

# --- LLM (phase 3) ---
anthropic>=0.40            # public Claude API (AnthropicProvider); dev on synthetic data
# anthropic[bedrock]       # for BedrockProvider -> Claude on AWS Bedrock GovCloud (CUI/FedRAMP/IL4-5)

# --- Artifacts (phase 4) ---
openpyxl>=3.1             # FISCAM 2024 test-plan .xlsx generator (fiscam_test_plan.py)
python-docx>=1.1           # Word SSP/POA&M
jsonschema>=4.0           # OSCAL validation against NIST schemas (validation.py)

# --- UI (phase 5) ---
streamlit>=1.40
reportlab
